{
  "name": "musicbyweather",
  "version": "1.0.0",
  "description": "A micro-service able to accept RESTful requests receiving as parameter either city name or lat long coordinates and returns a playlist (only track names is fine) suggestion according to the current temperature.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pedro Almeida Rocha",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  }
}
